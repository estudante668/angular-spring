import { Routes } from '@angular/router';
import { CursoComponent } from './cursos/curso/curso.component';

export const routes: Routes = [
    {
        path:'',
        component: CursoComponent,
        title: 'Curso Componente'
    }
];
